<%= @professor.name %>'s Students<p>

<% @professor.students.each do |student| %>
  <%= student.name %><br>
<% end %>

Average age of <%= @professor.name %>'s Students: <%= @professor.students.average_student_age %><br>
